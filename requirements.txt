# ============================================================
# Real Estate Hedonic Price Model — Python Dependencies
# Python 3.11+ required
# ============================================================

# --- Data Pipeline ---
pandas>=2.1.0
numpy>=1.26.0
requests>=2.31.0
python-dotenv>=1.0.0          # .env로 MOLIT API 키 관리
tqdm>=4.66.0                   # 대량 API 호출 프로그레스바

# --- Geospatial Stack ---
geopandas>=0.14.0
shapely>=2.0.0
pyproj>=3.6.0                  # 좌표계 변환 (EPSG:5186 ↔ 4326)

# --- Spatial Econometrics (PySAL) ---
libpysal>=4.8.0                # 공간가중행렬 생성
spreg>=1.4.0                   # SAR/SEM/SDM 추정기
esda>=2.5.0                    # Moran's I, LISA

# --- Classical Econometrics ---
statsmodels>=0.14.0            # OLS 베이스라인 + 진단
linearmodels>=5.3              # 패널/고정효과 (필요 시)

# --- Machine Learning (선택적 확장) ---
scikit-learn>=1.3.0
xgboost>=2.0.0                 # SHAP 기반 비선형 헤도닉 (옵션)

# --- Visualization ---
matplotlib>=3.8.0
seaborn>=0.13.0
folium>=0.15.0                 # 인터랙티브 지도
contextily>=1.4.0              # 베이스맵 타일

# --- Dashboard (dashboard/app.py) ---
streamlit>=1.40.0              # CSV 탐색 대시보드 (일별/구분별)
plotly>=5.20.0                 # 인터랙티브 차트

# --- Jupyter ---
jupyter>=1.0.0
ipykernel>=6.27.0
nbformat>=5.9.0

# --- Code Quality ---
black>=23.12.0
ruff>=0.1.9
pytest>=7.4.0
pytest-cov>=4.1.0
mypy>=1.7.0

# --- Geocoding (Kakao API 사용 시) ---
# kakao-api-wrapper          # 별도 설치 불필요, requests로 직접 호출
